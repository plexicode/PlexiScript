class KeyEvent {
    field type;
    field key;
    field isDown;

    constructor(isDown, key) {
        this.type = isDown ? 'KEY_DOWN' : 'KEY_UP';
        this.isDown = isDown;
        this.key = key;
    }
}
